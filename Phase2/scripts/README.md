# JSS and software-validation launchers

These scripts support package installation, source-archive checks, numerical
library setup, Metal measurements, and compiled cross-validation comparisons
for the future software article. Run them from the `Phase2` directory unless a
script documents another working directory.

They are not used to generate the CMPB cross-package performance claims.
